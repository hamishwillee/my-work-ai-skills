# MAVLink skills

Claude Code skills related to the MAVLink protocol.

- [`mavlink-command-review`](mavlink-command-review) — editorial review of mavlink-devguide pages documenting individual MAV_CMD commands or message fields (e.g. `mission_item_detail.md`), checking accuracy against the XML, citable evidence for implementation-specific claims, and change discipline.

Planned: a sibling skill for `en/services/*.md` protocol-level prose (upload/download sequencing, state machines) that isn't about one command's own parameters — the same change-control and no-XML-contradiction principles, applied to a different content shape.
